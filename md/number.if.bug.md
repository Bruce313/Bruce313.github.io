```javascript
var num = parse(Int, Float)("0", 10);
//!num == true;
//num !== 0 && isNaN(num)

var d = new Date(any);
if (d.prototype.toString() == "Invalid Date") {
}
//d has not method 'getTime'
```
